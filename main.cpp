#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "../UTS/model.h"
#include "../UTS/controller.h"



int main(){
    node *createNode = NULL;
    int cureNeeded, patients;
    scanf("%d %d", &patients, &cureNeeded);
    
    for(int i = 0; i <= patients; i++){
    scanf("%d %s %d - %s", &tgl, &bulan &tahun, %name);
    }
    return 0;
}